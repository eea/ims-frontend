version: '2.4'
services:
  backend:
    image: eeacms/ims-backend:develop
    ports:
    - "${BACKEND:-8080}:8080"
    environment:
      SITE: "Plone"
    volumes:
      - data:/data

volumes:
  data:
    driver: local
